import { IChildren } from "../../layouts/header/header.interfaces";

export interface IBounceWord extends IChildren {
    activateOnClick: boolean;
}

export interface IStyledBounceWord {
    activate: boolean;
}
